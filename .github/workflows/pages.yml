name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

**Step 5**
Scroll down to the bottom of the page. You will see **Commit new file** section:
- Leave the default message as is or type `Add GitHub Pages workflow`
- Make sure **Commit directly to the main branch** is selected
- Click the green **Commit new file** button

---

**Step 6**
Now go to the **Actions** tab on your repo — you will see **Deploy to GitHub Pages** appear in the left sidebar and a workflow run will start automatically.

Wait for the **green checkmark ✅** — your site is live at:
```
https://YOUR-USERNAME.github.io/municipal-edrms/
